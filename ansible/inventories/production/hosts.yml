---
all:
  children:
    invoice_servers:
      hosts:
        prod-server-1:
          ansible_host: prod1.example.com
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/production_key
