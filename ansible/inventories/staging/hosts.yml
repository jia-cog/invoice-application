---
all:
  children:
    invoice_servers:
      hosts:
        staging-server:
          ansible_host: staging.example.com
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/staging_key
