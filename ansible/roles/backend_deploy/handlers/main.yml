---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart invoice-backend
  ansible.builtin.systemd:
    name: invoice-backend
    state: restarted
    daemon_reload: true
