[production]
# Replace with your target server(s)
# Example:
# invoice-server ansible_host=192.168.1.100 ansible_user=deploy
#
# For local testing with Vagrant or a local VM:
# invoice-server ansible_host=127.0.0.1 ansible_port=2222 ansible_user=vagrant ansible_ssh_private_key_file=.vagrant/machines/default/virtualbox/private_key

[production:vars]
ansible_python_interpreter=/usr/bin/python3
