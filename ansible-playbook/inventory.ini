# Invoice Application - Ansible Inventory
#
# Update the hosts below with your actual server details.
# Example usage:
#   ansible-playbook -i inventory.ini site.yml

[production]
# Replace with your production server IP or hostname
# invoice-server ansible_host=192.168.1.100 ansible_user=deploy
# Example:
# invoice-prod-01 ansible_host=10.0.1.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[production:vars]
ansible_python_interpreter=/usr/bin/python3
