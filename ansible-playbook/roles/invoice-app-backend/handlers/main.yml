---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart invoice backend
  ansible.builtin.systemd:
    name: "{{ backend_service_name }}"
    state: restarted
    enabled: true
