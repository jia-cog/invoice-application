---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart invoice backend
  ansible.builtin.systemd:
    name: invoice-backend
    state: restarted
  become: true
