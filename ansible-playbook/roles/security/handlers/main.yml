---
- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
  become: true

- name: Restart invoice backend
  ansible.builtin.systemd:
    name: invoice-backend
    state: restarted
  become: true
